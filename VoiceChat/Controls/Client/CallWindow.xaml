<baseControls:ChatWindow x:Class="ChatControls.Client.CallForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:baseControls="clr-namespace:BaseControls;assembly=BaseControls"
             mc:Ignorable="d" 
             AllowsTransparency="True"
             Background="Transparent" 
             WindowStyle="None"
             ResizeMode="NoResize"
             MaxWidth="300" MinWidth="300" MaxHeight="100" MinHeight="100" d:DesignHeight="100" d:DesignWidth="300" WindowStartupLocation="CenterScreen" >
    <Border x:Name="windowBorder" BorderBrush="LightGray" BorderThickness="0.5" CornerRadius="6" 
            Background="#EDEFF0">
        <Grid x:Name="MainGrid">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="Info" TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Stretch" Grid.ColumnSpan="2" Grid.Row="0" Grid.Column="0"/>
        </Grid>
    </Border>
</baseControls:ChatWindow>
